# Claude instructions

## Git workflow

- Commit directly to `main` unless otherwise specified. Do not create a feature
  branch for commits in this repo by default.
- Only commit or push when asked.
